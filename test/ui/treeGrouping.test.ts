describe("Forming the tree structure", () => {

	it("Works for different grouping keys", () => {
		throw "TODO";
	});

	it("Works with >1 root node", () => {
		throw "TODO";
	});

	it("Works for roots that are already >1 level deep", () => {
		// really? would we do this? perhaps for files relative to the project root???
		throw "TODO";
	});

	it("Fills gaps in the hierarchy with empty nodes", () => {
		throw "TODO";
	});

	it("Fills gaps in the hierarchy with empty nodes, with >1 gapped branch", () => {
		throw "TODO";
	});

});