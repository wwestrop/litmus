{
  "name": "litmus",
  "version": "0.0.1",
  "description": "Litmus visual test runner for Javascript",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "tsc",
    "TODO0": "I just want a simple, repeatable, cross platform, string of commands",
    "TOOD1": "to be executed in a context where npm packages are available",
    "TODO1": "This would seem to be impossible without loads of crap. I just want SIMPLE!!!",
    "build": "run-s build1 build2 build3 build4 build5 build6 build7 build8 build9",
    "build1": "npx cash-mkdir -p bin/src/ui/css/",
    "build2": "npx cash-cp package.json bin/src/",
    "build3": "npx node-sass --recursive src/ui/css --output bin/src/ui/css/",
    "build4": "npx cash-cp --recursive src/ui/*.html bin/src/ui/",
    "build5": "npx cash-cp --recursive src/ui/*.png bin/src/ui/",
    "TODO7": "would be nice if cash-cp supported *.{a,b,c} expansion OR DO I HAVE TO MAKE THE DIR EXIST FIRST",
    "build6": "npx cash-mkdir -p bin/src/ui/css/",
    "build7": "npx cash-cp --recursive src/ui/*.js bin/src/ui/",
    "build8": "npx tsc",
    "build9": "npx tslint src/ts/**/*.ts",
    "TODO3": "Get the below dependency in a proper task runner so multiple targets share same prep-step",
    "pack-win": "run-s pack-win:0 pack-win:1",
    "pack-win:0": "npm run build",
    "pack-win:1": "npx electron-packager bin/src Litmus --arch x64 --icon ./res/Litmus.ico  --out dist --overwrite",
    "TODO4": "May need to bother about packaging node_modules (pruning devDeps, tree shaking would be nice too)",
    "TODO5": "shx rm, because cash-rm -rf doesn't seem to work here",
    "clean:0": "npx shx rm -rf dist",
    "clean:1": "npx shx rm -rf bin"
  },
  "keywords": [
    "litmus",
    "test",
    "unit",
    "testing",
    "mocha",
    "jasmine",
    "chai",
    "js",
    "javascript",
    "mock",
    "spy"
  ],
  "author": "Will Westrop",
  "license": "MIT",
  "devDependencies": {
    "@types/chai": "^4.1.2",
    "@types/mocha": "^2.2.47",
    "electron": "^1.8.2",
    "@types/node": "^9.4.0",
    "@types/react": "^16.3.13",
    "@types/react-dom": "^16.0.5",
    "cash-cp": "^0.2.0",
    "cash-mkdir": "^0.2.0",
    "cash-rm": "^0.2.0",
    "chai": "^4.1.2",
    "electron-packager": "^10.1.1",
    "node-sass": "^4.7.2",
    "npm-run-all": "^4.1.2",
    "npx": "^9.7.1",
    "parcel-bundler": "^1.7.1",
    "sass": "^1.0.0-beta.3",
    "serve": "^6.4.9",
    "shelljs": "^0.8.1",
    "shx": "^0.2.2",
    "ts-mockito": "^2.2.8",
    "tslint": "^5.9.1",
    "typescript": "^2.6.1"
  },
  "dependencies": {
    "mocha": "^4.1.0",
    "react": "^16.3.2",
    "react-dom": "^16.3.2",
    "stdlib": "^0.6.0"
  }
}
